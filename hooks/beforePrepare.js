#!/usr/bin/env node
module.exports = function(context) {
    const { exec } = require("child_process");
    /*

    */
    /*
    exec("", (error, stdout) => {
        if(error) {
            console.log(error.message + "");
            return;
        }
        console.log(stdout);
    });
    */
};
